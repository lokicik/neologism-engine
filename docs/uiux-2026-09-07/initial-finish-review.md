> Initial review, superseded by [finish-review.md](finish-review.md). The original full review is preserved verbatim below.

disposition: fix

Reviewer definition loaded from the supplied fallback path; review performed by a fresh independent agent without a browser. A formal five-block direction contract with FORM seed, QUALITY BAR cards, a decision critique-reference, and any pre-existing DESIGN.md were not supplied; the user's pinned existing-world contract governs this bounded review. An approved comp is not expected for this code-led build. Test files were sampled, not rerun; engine internals and non-supplied auxiliary surfaces were not reviewed.

## persistence

Pass. PRODUCT.md exists and records the primary explore → save → compare task, genuine local generation, discovery continuity, explicit preference feedback, English copy, and the limits of snapshot and structural-score evidence. Comp approval, reproduction state, and raster plate gates do not apply to the supplied code-led scope.

Evidence passes: opened all eleven required files. Create captures at 320, 390, 768, the actual user width of 1251, and 1440 show the document top and their complete lists. Saved captures at 390 and 1440 show their document tops; Details and Compare captures at both widths are valid viewport captures of the named dialogs. No required capture is missing or malformed. The close button is visibly present at the upper right of the current compare-390.png; the packet's concern about its absence is resolved by the file itself, not by the geometry test.

## fidelity

| Element or promise | State | Evidence |
| --- | --- | --- |
| TYPE | match | The supplied screenshots use the retained Inter and Space Grotesk hierarchy. Discovery names are 34px on desktop and 32px below 700px in web/src/ui.css:81 and :113. Both detector font warnings are explicitly overridden by the user's pinned font choice. |
| MATERIAL | match | Restrained digital surfaces, fine borders, and soft modal depth fit the pinned Operate app. No physical material, illustration, or raster asset was promised or imitated. |
| GROUND | match | web/src/ui.css:1 declares the pinned #0A0A0C ground, #141418 surface, #F2F2F5 text, and #6D56E8 accent; the screenshots and supplied measurements are consistent with those values. |
| First viewport and exploration | match | Every Create capture starts with the optional brief, one primary generation action, then real names. No method wall, finalist summary, score badge, crown, or training counter appears in the discovery list. The names carry the visual emphasis. |
| Responsive form | match | The captured list uses one column at 320/390, two at 768, and three at 1251/1440. The 1160px content cap and wrapping names are explicit in ui.css. Main controls remain legible and separated; measurements report no small targets or page overflow. |
| Navigation and method separation | match | Create, Saved, and Tools occupy the header. Sidebar.tsx exposes the four required Tools destinations; LabPage.tsx has one method selector and its own generation state. The open Tools menu and Lab were code-reviewed, not visually captured in this packet. |
| Continuous discovery and recovery | match | CreatePage.tsx separates draft and committed Config, keeps the prior list through failed replacement, gates automatic append on downward user intent, and retains an explicit Load more control. Discovery state is versioned and storage exceptions produce recoverable notices. The supplied tests support these behaviors; they are not a visual-quality score. |
| Details modal | match | Desktop captures show a right drawer over the existing grid; mobile shows the full-height dialog. NameDetails.tsx supplies native modal focus containment, Escape handling, and focus return without scrolling. Explanations and estimate limitations are visible; Name checks is an explicit action. |
| Details icon language | contradicted | The compass emoji visibly prefixes construction evidence in details-390.png and details-1440.png. NameCard.tsx:325 inserts it; :378 and :390 use Unicode disclosure chevrons, and :512 uses a text external-link arrow. These coexist with the drawn SVG action icons and violate the craft floor's glyph-icon rule. |
| Saved and comparison | match | Saved uses the same name-first cards, an unobtrusive search field, explicit comparison selection, and secondary Export. SavedPage.tsx preserves source order through case-insensitive filtering and caps selection at four. The desktop table presents actual construction, syllable, snapshot, and check evidence with no winner. |
| Mobile comparison reflow | adaptation | The focusable horizontal table region preserves the user-required side-by-side comparison at a narrow width; compare-390.png visibly continues into the next name column. NameComparison.tsx:13 names the scroll behavior for assistive technology, and ui.css:145 confines horizontal scrolling to the table. This is a responsive accommodation to the pinned comparison task. |
| Evidence and data truth | match | Details and comparison expressly distinguish local snapshot absence from availability and structural estimates from human preference. session-checks.ts retains completed, dated observations for comparison. The 500-card measurement is explicitly a rendering fixture, not naming-quality evidence. |
| Removal and preference continuity | match | App.tsx and saved-undo.ts restore only removed sources into current collections and preserve later choices. Search, scrolling, and comparison selection do not invoke feedback actions. |
| Floor and motion | match apart from icon row | No eyebrow, gradient text, hard block shadow, decorative glass, or side stripe appears in the supplied primary surfaces. Focus treatment is visible in Details; ui.css covers selection, caret, and reduced motion. Primary text/control contrast measurements exceed 4.5:1. |

## ceiling

Reached within the pinned existing-world scope: spacious composition, name scale, restrained borders, and concentrated accent already support the task. No imagery, ornament, font replacement, or new visual-world exploration is warranted. A separate QUALITY BAR card was not supplied, so no claim is made against an unseen card.

## material_fixes

1. Floor / Details icon language: remove the compass prefix or replace it with a small SVG from the existing icon language, and replace the Details disclosure/external-link glyphs with matching SVGs in web/src/components/NameCard.tsx:325, :378, :390, and :512; preserve the wording and control geometry, then recapture the same Details files for the verdict pass.

## keep

Keep the Auto-first single discovery list, large names, pinned dark palette and fonts, generous spacing, explicit Save/Compare actions, evidence disclaimers, modal focus behavior, and separation of research from naming-quality claims.
